# Project Rules

- The agent must only create or modify files and components when explicitly commanded by the user.
- Do not proactively create extra helper components, primitive UI files, or utility folders without direct instruction. Only build what is requested in each step.
